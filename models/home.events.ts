import { Event } from './shared';
/**
 * Home Page Events
 * @route /events
 * @description high level summary of events upcoming with ctas to register / login
 */
interface HomeEvents {
    Events: Array<Event>
}

